import { sign } from "crypto";

export const locators = {
    emailTextfield: '#session_key',
    passwordTextfield: '#session_password',
    loginWithEmail: 'home-hero-sign-in-cta',
    signInButton: 'Sign in',
}